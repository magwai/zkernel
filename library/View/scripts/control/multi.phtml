<?php

$model = new Default_Model_Lang();
$list = $model->fetchAll(null, 'orderid');
$reg = Zend_Registry::get('Zkernel_Multilang') ? Zend_Registry::get('Zkernel_Multilang') : '';

?>
<div id="c_lang"><select onchange="window.location = this.value">
<?php

if (count($list)) foreach ($list as $el) {
	$d = explode(' ', @$el->domain);

?>
	<option<?php echo $reg->id == $el->id ? ' selected="selected"' : '' ?> value="<?php echo @$el->domain ? 'http://'.$d[0] : '/'.$el->stitle ?>/control"><?php echo $el->title ?></option>
<?php

}

?>
</select></div>